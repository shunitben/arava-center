(function (Drupal, $) {
    'use strict';

    Drupal.behaviors.arava_assignments = {
        attach: function (context) {
// working very badly!
//            $('#edit-checker').chosen();

        }



    }
}(window.Drupal, window.jQuery));
